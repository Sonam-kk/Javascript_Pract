// Calculating the area of circal radius is given.
// Map method
array=[1,2,3,4,5];
const outputmap=array.map((i)=>Math.PI*i*i);
console.log(outputmap);
// Filter mathod
array1=[1,2,3,4,5,12,18,16];
const outputfilter=array1.map((i)=>i%6===0);
console.log(outputfilter);
